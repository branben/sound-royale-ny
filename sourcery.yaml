# Sourcery configuration for Sound Royale (free tier)
# Only exclude patterns are customized; all default rules active.

exclude:
  - backend/game_engine/migrations
  - backend/media
  - src/components/ui
  - src/data
  - tests
  - tests/e2e
