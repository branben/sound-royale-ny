# Visual-verify harness: ignore per-run captures + generated report.
# Keep baselines/ committed as the golden reference set.
captures/
report.json
# Generated at capture time (contains live JWT creds):
seed.json
