#!/bin/sh
npx lint-staged
npm run verify:types
# E2E guard runs structure validation (no backend needed — scripts/e2e-guard.sh
# is tracked via .gitignore exception !scripts/e2e-guard.sh)
npm run test:e2e:guard
