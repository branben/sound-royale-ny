#!/bin/sh
npx lint-staged
npm run verify:types
# NOTE: test:e2e:guard (scripts/e2e-guard.sh) omitted from pre-commit:
# scripts/* is gitignored, so the script can never exist in a clean checkout
# and the hook would fail for every commit on origin/main. Run it manually/CI.
